import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.certifyyaself.netflix',
  appName: 'netflix',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
